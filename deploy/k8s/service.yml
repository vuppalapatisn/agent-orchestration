apiVersion: v1
kind: Service
metadata:
  name: agent-orchestration
  namespace: agent-orchestration
spec:
  selector:
    app: agent-orchestration
  ports:
    - name: http
      port: 80
      targetPort: 8080
