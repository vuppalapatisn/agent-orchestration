global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'agent-orchestration-production'
    metrics_path: '/actuator/prometheus'
    basic_auth:
      username: agentadmin
      password: changeit-changeit
    static_configs:
      - targets: ['host.docker.internal:8080']
